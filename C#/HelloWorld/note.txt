// Create a new project
dotnet new console -o csharp 

// Comments:
    // or /*    */

// Import other namespaces
using System;

namespace HelloWorld
{
    class Program
    {
        
        static void Main(string[] args)  //CLR executes Main method
        // void = return type
        // Main = method name
        // (input_type input)
        {
            Console.WriteLine("Hello World!");
        }
    }
}



// Declare variables
int number;
int number = 1;
const float Pi = 3.14f;
decimal number = 1.2m;

Primitive types: 
    integral number: byte, short, int, long
    real number: float, double, decimal
    char 
    bool 
Non primitive types:
    String, Array, Enum, Class


// Check overflowing (number exceeding the data type)
    checked
    {
        byte number = 255;
        number = number + 1;
    }



// Variables cannot be accessed outside the code block

// Compile: Ctrl-Shift-B

// Delete: Ctrl-X


// Converrsion:
    - implicit type conversion 
        {
            byte b = 1;
            int i = b; 
            //no data loss
        }
    - explicit type conversion (casting)
        {
            int i = 1;
            byte b = i; //data loss --> won't compile
            byte b = (byte)i;
        }
    - conversion between non-compatible types
        {
            string s = 'i';
            int i = Convert.ToInt32(s); //ToByte(), ToInt16(), ToInt32, ToInt64,
            int j = int.Parse(s);
        }


// Debug using try-catch block
    try
    {
        //code you wanna try
    }
    catch ()
    {
        //error message
    }



// Operators
    - Arithmetic:
        +, -, *, /, % (remainder), ++ (increment), -- (decrement)
        (a++, ++a)
    - Comparison:
        ==, !=, >=, <=
    - Assignment:
        = 
        +=  (a+=3  --> a=a+3)
        -=, *=, /=
    - Logical operators:
        &&
        ||
        ! (not)
    